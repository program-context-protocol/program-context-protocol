# Program Context Protocol (PCP)

Structured project state, deterministic CI gates, and strategy validation for
LLM-driven development — prevents context drift and hallucinated "done" state
across long-running agent sessions.

## Install
pip install -e .
pcp init

## License
MIT OR Apache-2.0
